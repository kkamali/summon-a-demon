<%= form_for @summon do |f| %>
  <%= summon_fields @summon %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= collection_select(:summon, :sacrifice_id, Sacrifice.all, :id, :name) %>
  <%= f.submit "Summon The Demon" %>
<% end %>